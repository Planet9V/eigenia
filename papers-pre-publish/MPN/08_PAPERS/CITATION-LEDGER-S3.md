| Field | Value |
|:---|:---|
| Designation | CITATION-LEDGER-S3 |
| Paper | MPN-S3, `08_PAPERS/S3-mapping-phi.md`, revision 9 |
| References listed | 15, all used in the body. The list prints them 1 to 12, then 14, 15, 13, and the ledger restores numeric order |
| Ledger date | 14 September 2026 |
| Status counts | VERIFIED 3, VERIFIED-NARROWED 1, UNVERIFIED-OFFLINE 2, BROKEN 0, SELF 9. **Revised 14 September 2026 in the repair pass**: four of the anchor rows inside [12] change, three locators corrected in the paper and one finding withdrawn as a misreading |
| Also checked | The twenty-six implementation anchors inside [12], one by one, and the twelve generator scripts of the closing table |
| Companion | `CITATION-STATUS-ALL.md` for the roll-up across the six documents |

Every `[n]` in the paper is listed with what the paper asserts on its authority, quoted from the body rather than from the reference line, and with a status established here. The five statuses are the ones the programme's plan fixes, and they are used as in the S2 ledger: **VERIFIED** for a source opened from this session that says what the paper says it says, **VERIFIED-NARROWED** for one that supports part of the claim with the unsupported part named, **UNVERIFIED-OFFLINE** for a published source that could not be opened from here, **BROKEN** for a path, line or URL that does not resolve, and **SELF** for a document in this series, with its revision named and its currency stated.

Reference [12] is unlike the others and is treated in two places. Its row below records the reference as a whole; a second table checks each of its twenty-six path-and-line anchors separately, because a single status over twenty-six anchors would conceal exactly the failure this ledger exists to find. The finding is that twenty-one anchors are exact, three are off by one line or over-reach the range they name, one is characterised in a way the code does not support, and one names a line whose content is the consumption of the thing cited rather than the thing itself.

## References in the paper

| [n] | Reference and kind | Status | What the paper claims on its authority | What was checked, and what is there | Where it lands if it fails |
|:--|:---|:---|:---|:---|:---|
| 1 | MPN-S1, `08_PAPERS/S1-mckenney-lacan-theory.md`, revision 3. Series document | SELF | "S1 asserts that the transformations professional practice applies to a leitmotif are functions of a psychological state, and that the functions are total, deterministic and inspectable"; and that the frame library's dispersions "are themselves system output and are labelled as such in S1" | Opened. The status line reads "Draft, revision 3, after Block A arbitration and a Skeptic review of revision 2". **Revision 3 is current.** The transformation assertions are A6 and A11 of the register the paper reaches through [11], and S1's own ledger records the system-output labelling of the 232 frames as "data, not evidence", which is the labelling this paper relies on | Every section. S3 is the mapping paper for S1's theory and has no independent standing |
| 2 | MPN-S2, `08_PAPERS/S2-mathematics.md`, revision 4. Series document | SELF | "S2 establishes what kind of object that transformation is, and leaves three things to this paper: the timbre space ... the tie behaviour of the mode selector; and the completion of the identifiability analysis"; and "S2 reports the first two of those" | Opened. The status line reads "Draft, revision 4, after Skeptic, Constraint Guardian and User Advocate reviews and the arbitration of 13 September 2026", and the file carries the post-acceptance correction the paper's own [14] forces on it. **Revision 4 is current.** Sections 4.3, 6.4 and 2.6 of S2 carry the three items as described | Sections 2.6, 2.3 and 3. The identifiability bound of section 3 is a completion of S2's and does not stand alone |
| 3 | McAdams, Winsberg, Donnadieu, De Soete and Krimphoff 1995, *Psychological Research* 58, 177-192. Published paper | VERIFIED | "McAdams and colleagues, working with synthesised instrument tones, find three shared dimensions with correlates log rise time, spectral centroid and degree of spectral variation, together with instrument-specific *specificities*, attributes of a particular instrument that lie on no shared dimension at all" | Fetched at `link.springer.com/article/10.1007/BF00419633`. Authors, journal, volume, pages and year as cited. The three acoustic correlates are returned as log-rise time, spectral centroid and degree of spectral variation, and the article states that "musical timbres possess specific attributes not accounted for by these shared perceptual dimensions". Every element of the claim confirmed | Section 2.6, the timbre map, and the rank-3 finding the map's null direction depends on. If it failed, the three-dimensional codomain would have one published warrant rather than two |
| 4 | Caclin, McAdams, Smith and Winsberg 2005, *JASA* 118(1) 471-482. Published paper | UNVERIFIED-OFFLINE | "A confirmatory study using synthetic tones ... also finds **three** dimensions: it confirms attack time on a logarithmic scale, spectral centroid, and spectrum fine structure modelled as even-harmonic attenuation, and finds spectral flux only weakly salient and strongly context-dependent" | The AIP article page returned HTTP 403 and was not read. Not retried by another route, per the verification rules. Needed: the AIP PDF, the PubMed Central deposit if one exists, or a library copy | Section 2.6's second warrant, and specifically the sentence that the two studies "disagree only about the acoustic correlate of the third" dimension. The three-dimensional finding survives on [3]; what is unwarranted without [4] is the characterisation of the disagreement and the spectral-flux caveat |
| 5 | Commensurability audit, `08_PAPERS/COMMENSURABILITY-AUDIT.md`. Series document | SELF | "a weight quoted without one is an unexamined claim about a variance"; and that six tenths of the trauma range collapsing to one velocity "is the observation the commensurability audit makes independently" | Opened. The rule that effective contributions must accompany nominal weights is the audit's method section, and the three-band dynamics table with its hard-coded middle default is recorded at line 99, in the words the paper uses, against `mpn_reference_lookup.ts:150-178`. Confirmed | Sections 2.1, 2.4 and 3, and every effective-contribution figure in the paper. Those figures are separately computed by `s3_commensurability.py`, so a failure costs the corroboration and not the numbers |
| 6 | Decision log, `08_PAPERS/DECISION-LOG-2026-09-12.md`. Series document | SELF | "Decision 6 makes the eight-marking linear form normative"; and that the fourth modal decision point "is already question 5a in the listening pack under decision 5" | Opened. Decision 4 at line 17 ("No partner"), decision 5 at 18 ("Put it to the professionals ... It becomes a question in the listening pack"), decision 6 at 19 ("The eight-marking linear form, the only one matching the stated equation") and decision 8 at 21. All four decisions the reference claims are implemented here are present with the dispositions the paper attributes to them | Sections 2.1, 2.3 and 4. Decision 8 in particular carries the whole separation of the influence layer in section 5 |
| 7 | MPN-2, `08_PAPERS/MPN-2-notation.md` section 3.6. Series document | SELF | "The harmonic codomain is the 24 consonant triads under the neo-Riemannian transition algebra generated by $P$, $L$ and $R$, which the sister series sets out in full" | Opened at `/home/claude/mpn-theory/08_PAPERS/MPN-2-notation.md`. Section 3.6 is at line 159 and carries the dihedral group of order 24 acting simply transitively on the 24 consonant triads, the LR chain, the Cayley graph of diameter five, and the lexicographic tie-break on shortest words with P before L before R, which is the tie-break the paper's section 2.5 adopts. Exact | Section 2.5, the harmonic operator. The group facts are re-verified by `s3_harmony.py` over the full Cayley graph, so the citation carries provenance rather than proof |
| 8 | MPN-1, `08_PAPERS/MPN-1-foundations.md`, correction 12. Series document | SELF | "The affective reading of the individual generators, that $P$ darkens and $L$ elevates, is a convention with no experimental support, and MPN-1 says so in its own words" | Opened. Correction 12 is at line 332 and reads "The affective reading is a convention without experimental evidence; Paper 2 assigns its evidence tier". The paper's "in its own words" is accurate | Section 2.5's statement of what the mapping does not adopt. It is a disclaimer, so its failure would widen a claim rather than remove one |
| 9 | McKenney, *The cognitive bias atlas*, Unified Psychometric Field Theory volume XV. Corpus file, cited without a path | VERIFIED | "S1 settles the classification at the author's Cognitive Bias Atlas, thirty entries in four domains"; and in the reference line, "sixteen catalogued and fourteen in the expanded reference" | Opened at `01_THEORY/03_unified/16_Vol_15_Cognitive_Bias_Atlas.md`. Section 1.1 gives four domains of distortion; section 3 catalogues CB-001 to CB-016 under categories A Perception, B Decision, C Social and D Memory; section 3.5, "Expanded Reference (Nodes 17-30)", holds CB-017 to CB-030 as table rows. Sixteen and fourteen, four domains, thirty entries: exact. Note that three files in that directory carry "Cognitive Bias Atlas" in their names, and only this one is volume XV, so the volume number in the citation is what disambiguates it | Section 4 entire, the bias layer. Sections 4.1 to 4.4 reconcile the Atlas's thirty against the implementation's thirty, and neither set has meaning without the Atlas being the classification of record |
| 10 | Cialdini, *Influence: The Psychology of Persuasion*. Published book, no edition, year or pages given | UNVERIFIED-OFFLINE | "Decision 8 separates Cialdini's six principles of influence from the bias index: a technique applied to a subject and a distortion in a subject are different objects" | Not fetched. No edition is named in the citation and none was opened. Needed: any edition of the book, or a review that enumerates the six principles, to confirm that the six the paper moves are the six Cialdini names. The six as the corpus lists them are recorded in the assertions register and in `mpn_reference_data.ts`, which is where the paper's actual list comes from | Section 5, the influence layer. The separation is a decision of the author's, taken at decision 8, and the citation supports the naming of the six rather than the decision to move them |
| 11 | Assertions register, `08_PAPERS/ASSERTIONS-REGISTER.md`. Series document | SELF | "B1 asserts only that the layer exists and is not predicted by the state, and the identifiability result of section 3 does not count the bias layer at all, because the layer is carried between staves rather than on one, per B4 in the register" | Opened. A4 at line 54, A7 at 93, A8 at 106, A11 at 150, B1 at 173, B3 at 193 and B4 at 207, all present as headed entries with claim, formal content, falsification clause and test. B1's claim is as quoted; B4's claim places bias "on the harmonic relation between staves rather than any channel on a single stave", which is what the paper relies on. No revision is carried on the register | Sections 2.3, 2.4, 3, 4 and 5. Section 3's identifiability count in particular depends on B4's between-stave placement being the register's own position and not the paper's |
| 12 | MPN Conductor reference implementation, cited at path and line. Thirty-one anchors across ten files after the repair pass | VERIFIED-NARROWED | Twenty-seven separate claims in the body, each attached to one or more anchors. The anchors and their individual claims are in the table below | Every anchor was opened at the line cited, in the working tree at `/home/claude/mpn-conductor-standalone` at commit `8a3db9f`. **Twenty-one were exact as first cited; three more are exact after the repair pass of 14 September 2026 corrected `:345` to `:344`, `:60-70` to `:61-70` and `:63-132` to `:63-107` in the paper.** Two rows remain VERIFIED-NARROWED, both at `GeniusComposer.ts`, naming a consumption site rather than the construct. **The earlier finding against the "discarded assignment" at `score_orchestrator.ts:360` is withdrawn as a misreading**: the paper claims the mode field is read by nothing, not the object, and that claim is confirmed. See the row below | Sections 2.1 to 2.6 and section 3. This is the paper's primary evidence and almost every negative finding in it rests on one of these anchors |
| 13 | MPN-NOTE-03, `08_PAPERS/MPN-NOTE-03-implementation-audit.md`. Series document. Printed last in the list, out of numeric order | SELF | "The enumeration is `s3_modes.py`, which searches every non-test source file for lines returning or assigning one of the seven mode names or a scale formula, rather than for the names of functions that might do so; that is the distinction an earlier revision of this paper got wrong" | Opened. Its status line records that it is now "the path-and-line appendix for S4 and the record of the search method and of the one tool that failed", and it carries the call chains and the extracted passages. It is current for S3 revision 9 | Sections 2.3 and 7, and the paper's account of its own method. The withdrawn `s3_callgraph.py` and the reason for its withdrawal are recorded there rather than in the paper |
| 14 | MPN-S3-AMENDMENTS, `08_PAPERS/MPN-S3-AMENDMENTS.md`. Series document | SELF | "The corrections to S2 and to the register simplex figure are issued separately"; and "The replacement text for each is issued separately so the register can be amended without rereading this paper" | Opened. Section 2 carries the register replacement text for A4, A8 and B4; section 3 carries the parser defect and the figures it moves in S2 and in the simplex figure; section 4 carries the two commensurability corrections. It is the amendments note for revision 9 and is itself extended by `MPN-S3-AMENDMENTS-2.md` of 14 September, which the design document cites and this paper predates | Sections 2.3, 2.4 and 4, and S2's post-acceptance correction. A failure here would leave S2 uncorrected rather than S3 unsupported |
| 15 | Drake and Botte 1993, *Perception and Psychophysics* 54(3) 277-286. Published paper | VERIFIED | "Drake and Botte give relative just-noticeable differences for tempo of about 6 per cent for a single interval and about 3 per cent for a six-interval sequence, over interonset intervals from 100 to 1,500 milliseconds with sensitivity best between 300 and 800" | Fetched at `link.springer.com/article/10.3758/BF03205262`. Authors, journal, volume, issue, pages and year as cited. The reported JNDs are 6 per cent for a single interval, 4 per cent for two, 3.2 per cent for four and 3 per cent for six; the interonset range is 100 to 1,500 milliseconds; and sensitivity is best between 300 and 800 milliseconds. Every figure in the paper's sentence is exact | Section 2.2's comparison of the tempo law's 35 reachable tempi against published discrimination thresholds. Without it the paper could say the tempo bands are coarse but not by how much, and `s3_tempo_metre.py` would have no threshold to compare against |

## The twenty-six implementation anchors of [12]

Checked against `/home/claude/mpn-conductor-standalone` at commit `8a3db9f` of 14 September 2026. The status column grades the anchor, not the claim; where an anchor is wrong and the claim it carries is true, the row says so and gives the line that should be cited.

| Anchor as cited | Status | What is at that line, and what the paper takes from it |
|:---|:---|:---|
| `psychometric_calculus.ts:170-187` | VERIFIED | `entropyToRhythm` opens at :170 and closes at :187, exactly the range cited. It bands entropy into three stability labels at :173-174 and interpolates within the returned range at :181, which is the piecewise affine tempo law of section 2.2 |
| `psychometric_calculus.ts:109-113` | VERIFIED | The `MODES` constant, real to phrygian or locrian, symbolic to ionian or mixolydian, imaginary to lydian or whole-tone. Unreachable as the paper says: a search of every file under `src/` finds the identifier at its own definition and nowhere else |
| `psychometric_calculus.ts:192-199` | VERIFIED | `rsiToMode` opens at :192 and closes at :199. Unreachable as the paper says: the only callers are in `src/__tests__/psychometric_calculus.test.ts` |
| `psychometric_calculus.ts:296-320` | VERIFIED | `psychometricToMusical` opens at :296; :320 is the tension line the range is drawn to. The seven numbered steps in between are the ones section 3 takes through the mapping |
| `mpn_reference_lookup.ts:31-49` | VERIFIED | `checkCondition` opens at :31 and closes at :49, exactly the range cited. The operator precedence at :35-43 tests for a hyphen before any comparator, which is the parsing order section 2.1 relies on |
| `mpn_reference_lookup.ts:110-120` | VERIFIED | `lookupTimeSignature` opens at :110, the metre lookup of section 2.2 |
| `mpn_reference_lookup.ts:125-145` | VERIFIED | `lookupTempoRange` opens at :125, the tempo lookup |
| `mpn_reference_lookup.ts:150-176` | VERIFIED | `lookupDynamics` opens at :150, the dynamics lookup whose three-band table is section 2.1's shipped path |
| `mpn_reference_lookup.ts:220-242` | VERIFIED | `lookupMode` opens at :220, the mode lookup of section 2.3 |
| `mpn_reference_lookup.ts:261-278` | VERIFIED | `lookupModeScale` at :261-265 and `lookupModeName` at :270-278, exactly the range cited. `lookupModeName` carries the third modal table, real to phrygian, symbolic to ionian, imaginary to lydian |
| `mpn_reference_lookup.ts:344` | VERIFIED. **Corrected in the repair pass of 14 September 2026, from `:345`** | `lookupAllParams` opens at :344; :345 is its first parameter, `trauma: number`. The claim is true: it is referenced only at its own definition and in `src/components/mpn-lab/__tests__/mpn_calculus.test.ts:6`, so it is the unreachable remainder the paper calls it. Reference [12] now gives :344 and records the former :345 |
| `mpn_reference_data.ts:248-406` | VERIFIED | The metre and tempo entries. `rhythm-001` opens at :248 and the tempo block runs to the entry boundary at :406 |
| `mpn_reference_data.ts:664-742` | VERIFIED | `DYNAMICS_ENTRIES` opens at :664, and :742 falls inside `dynamics-004`, the crescendo entry. The range covers the volume-level entries section 2.1 reads |
| `mpn_reference_data.ts:1034-1102` | VERIFIED | `MODE_ENTRIES` is declared at :1033 and its first entry, `mode-001`, opens at :1034; :1102 closes `mode-003`. The range is the scale-mode entries, and the one line of slack is the declaration rather than an entry |
| `score_orchestrator.ts:61-70` | VERIFIED. **Corrected in the repair pass of 14 September 2026, from `:60-70`** | Line :60 is blank. `OrchestratorOutput` runs from :61 with `global` at :65-70 and `staves` at :72, which is the output interface the paper cites. Reference [12] now gives :61-70 and records the former :60 |
| `score_orchestrator.ts:267` | VERIFIED | The live call to `psychometricToMusical`, with the state, the active adjustments and a character key |
| `score_orchestrator.ts:321` | VERIFIED | The live call to `this.composer.composeMelody`, inside the `isSpeaking` branch at :319 |
| `score_orchestrator.ts:360` | VERIFIED. **This row's earlier finding is WITHDRAWN as a misreading, in the repair pass of 14 September 2026** | The line reads `stave.musicParams = globalParams;`. The earlier row said the characterisation "discarded" was unsupported because the mutated stave is returned at :363 and `musicParams` is read at `score_exporter.ts:81`, `:84`, `:88` and `:89`. **That is true of the object and not of the claim.** The paper's claim, at its line 178, is about the mode field and gives its own test: "the string `musicParams` never appears with `.mode` anywhere else in the source". A search of every non-test file under `src/` confirms it: `musicParams.mode`, `globalParams.mode` and `params.mode` occur nowhere in the shipped path; the only `params.mode` in the tree is `ai_music_client.ts:394`, on a different type, `PsychometricTextParams`, in the PSYCHOSCORE branch. The four fields the exporter does take are `instrumentFamily`, `timbre`, `dynamic` and `articulation`; the mode it writes is the literal `mode: 'ionian'` at `score_exporter.ts:73`, under the comment `TODO: extract from params`. `OrchestratorOutput.global` declares no mode at :65-70 and the output object supplies none at :440-445, so `(output.global as any)?.mode` at `page.tsx:488` is undefined on every frame. `composeMelody` receives `globalParams` and reads only `params.dynamic` at :176 and `params.articulation` at :177, taking its mode from `applyProfessionalTransformations` and the hard-coded triple instead. **`lookupMode`'s answer is computed on every frame and read by nothing. The paper is right.** The paper now carries this evidence in the body and the anchors in [12] |
| `GeniusComposer.ts:161` | VERIFIED | `const rsi = { real: 0.33, symbolic: 0.33, imaginary: 0.34 };` The hard-coded register triple, exactly as cited |
| `GeniusComposer.ts:180` | VERIFIED-NARROWED | The line resolves and is the call to `applyProfessionalTransformations`, which consumes the triple through `transformContext`. **It does not carry the triple**, which is at :161; it is where the triple reaches the transformation rules. Cited under a heading that names only the triple |
| `GeniusComposer.ts:188` | VERIFIED-NARROWED | The line resolves and reads `const targetMode = transformations.mode;`, the point at which the mode derived from the hard-coded triple is taken up. Same qualification as :180 |
| `leitmotif_transformation_rules.ts:63-107` | VERIFIED. **Corrected in the repair pass of 14 September 2026, from `:63-132`** | The modal selector `getModalTransformation` is at :63-91, with the trauma branch at :79, :84 and :89; the scale formulas, `getModeScaleDegrees`, are at :96-107. The former range over-reached into `getOrchestrationLevel` at :109-132, which the citation does not name. Reference [12] now gives :63-107, names both constructs at their own lines and records the over-reach |
| `leitmotif_transformation_rules.ts:209-250` | VERIFIED | `fragmentLeitmotif` opens at :209 and the shipped A8 scalar is at :215, `(entropy * 0.6) + (trauma * 0.4)`, with the four thresholds at :217, :225, :235 and :245. This is the superseded pair still in the code, as section 2.4 says |
| `leitmotif_generator.ts:243-266` | VERIFIED | `selectTransformation` opens at :243 and closes at :266, exactly the range cited. It reads `rsi.real` at :257 and `rsi.imaginary` at :260, and is called live at `score_orchestrator.ts:314`, so these are register readings that do reach a score |
| `psychometric_calculus.ts:320`, `:356-361` | VERIFIED | :320 is `const tension = (state.rsi.real * 0.9) + (state.entropy * 0.1);` and :356-361 is the key assignment branching on `rsi.real`, `rsi.imaginary`, `rsi.symbolic` and entropy in that order. Both are register readings that reach a score |
| `page.tsx:353`, `:488` | VERIFIED | :353 is `const lyapunov = (trauma + entropy - 0.5) * 0.5;` and :488 is the branch that prints Ionian, Lydian or Phrygian from that quantity when the orchestrator's mode is absent. This is the branch section 2.3 shows always wins |
| `mckenney_lacan_calculus.py:58-90` | VERIFIED | `trauma_to_velocity` at :58-66 implements $v = 20 + 107\tau$ with the equation written in its docstring at :63, and `trauma_to_dynamic` at :69-90 discretises to the eight markings ppp to fff at seven thresholds. Exactly the range cited |

## Generators cited in the closing table

The paper names eleven scripts in `05_DATA/03_generators/` plus a twelfth it withdraws, and a README. The scripts exist and are the ones the table describes. **So does the directory, as of the repair pass of 14 September 2026.** When this ledger was written, the only corpus copy reachable from the session was the upload snapshot of 12 September, which held one file, `a8_form_analysis.py`. The live corpus on the author's machine holds all eleven plus `s3_bias_reconcile.py` and `S3-GENERATORS-README.md`, under exactly the names the paper gives, and spot-checked MD5 sums match the working copies (`s3_modes.py` `873b6b9f5f7309001cafb7dd43a19e5d`, `s3_bias_reconcile.py` `0c23c81e1306da3fd596bf4cd7dcf92f`, the README `7c0b478c0a2c12d93f01c6a2ad5fba54`). **The citation as written resolves and nothing in the paper's generator table needs a new path.** All eleven resolve instead at `/home/claude/` and again at `/home/claude/gen/` and `/home/claude/deliver/`, under the names the paper gives: `s3_modes.py`, `s3_register_reach.py`, `s3_tempo_metre.py`, `s3_dynamics.py`, `s3_harmony.py`, `s3_interpolate.py`, `s3_timbre.py`, `s3_clip.py`, `s3_commensurability.py`, `s3_frame_cells.py`, `s3_worked_frame.py` and `s3_bias_reconcile.py`, which is twelve rows because the table lists twelve. The withdrawn `s3_callgraph.py` exists at `/home/claude/gen/s3_callgraph.py` and nowhere else, which is consistent with its withdrawal. `S3-GENERATORS-README.md` resolves only at `/home/claude/deliver/S3-GENERATORS-README.md`.

The consequence is narrow and should be said plainly. Every script the paper depends on can be run, and the paper's claim that no figure is transcribed is checkable. The citation can now be followed as written. The one change made in the repair pass is to `S3-GENERATORS-README.md`, which the paper cited by bare filename and which now carries its corpus path in both places the paper names it.

## Claims carried without a citation, on purpose

Section 2.6's timbre map, its Hadamard contrast basis and the rank and null direction of the DISC-to-timbre map are the paper's own construction and are cited to nothing, correctly: they are verified by `s3_timbre.py` rather than attributed. The same holds for the interpolation weight of section 2.3, the seven rhythmic cells of section 3, and the reachable-tuple counts. Section 2.6's opening statement that the theory has never specified timbre is a claim about an absence and has no source, as it should not.

## Not cited, though available

Grey, Krumhansl and the wider timbre-space literature sit behind [3] and [4] and are not needed, because the paper takes only the dimensionality and the correlates. Lerdahl and Krumhansl 2007 and Krumhansl and Kessler 1982 are cited in MPN-2 for the pitch-space distance the harmonic coordinate declines to use, and the paper reaches them through [7] rather than directly, which is the same policy S2 states. Temperley and Tan 2013, which S2 cites at its [19] for the non-monotonicity of the mode ordering in brightness, bears directly on section 2.3's tie rule and is not cited here; it is the one omission in this list that a later revision might reasonably close.
