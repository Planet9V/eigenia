| Field | Value |
|:---|:---|
| Designation | CITATION-LEDGER-DESIGN |
| Document | MPN-DESIGN-01, `08_PAPERS/DESIGN-UNIFIED-FRAMEWORK-rev8.md`, revision 8 |
| References listed | 21. Twenty are used in the body; [7] appears in the reference list and nowhere else |
| Ledger date | 14 September 2026 |
| Status counts | VERIFIED 7, VERIFIED-NARROWED 3, UNVERIFIED-OFFLINE 0, BROKEN 0, SELF 11. **Revised 14 September 2026 in the repair pass**: [1], [5], [7] and [21] are corrected in the document; [7] and [21] move to VERIFIED, [1] and [5] move to VERIFIED with the misattributions named in their own reference lines; [9] to [13] are repointed from the `gen6/` working copy to the corpus prefix, which now resolves |
| Also checked | The implementation anchors inside [21], one by one, and the five generator and verification files of [9] to [13]. **In the repair pass of 14 September 2026 the live corpus generators directory on the author's machine was listed directly**, rather than the upload snapshot of 12 September this ledger was first written against |
| Companion | `CITATION-STATUS-ALL.md` for the roll-up across the six documents |

Every `[n]` in the document is listed with what it asserts on its authority, quoted from the body rather than from the reference line, and with a status established here. The five statuses are used as in the other ledgers: **VERIFIED** for a source opened from this session that says what the document says it says, **VERIFIED-NARROWED** for one that supports part of the claim with the unsupported part named, **UNVERIFIED-OFFLINE** for a published source that could not be opened from here, **BROKEN** for a path, line or URL that does not resolve, and **SELF** for a document in this series, with its revision named and its currency stated.

Two things about this document shape the ledger. It is the only one of the five whose reference list already carries its own verification apparatus, in [13], and that apparatus is good: the corrections it records were checked here and are real. And it is the one that revises its own citations as part of its change audit, correcting the generator path prefix at [9], [10] and [11] that revision 7 got wrong. What this pass added was the cases where that self-correction did not reach, which was [7], where the document recorded a path as unresolved that in fact resolves; [21], where three anchors named the wrong file, the wrong line or the wrong range; and [1] and [5], where the document attributed to a series paper a figure or a section that is not in it. **All four are repaired in the document, in the repair pass of 14 September 2026, and each repair is recorded in its row below.** One thing that pass established changes the shape of the whole ledger: the corpus generators directory, empty of these scripts in the 12 September upload snapshot, now holds every script the five documents cite, so revision 8's own correction of the prefix to `gen6/` is itself superseded and [9] to [13] are repointed back to `05_DATA/03_generators/`.

## References in the document

| [n] | Reference and kind | Status | What the document claims on its authority | What was checked, and what is there | Where it lands if it fails |
|:--|:---|:---|:---|:---|:---|
| 1 | MPN-S1, `S1-mckenney-lacan-theory.md`, revision 3, with `MPN-S4-AMENDMENTS.md`. Series document. **Reference line rewritten in the repair pass of 14 September 2026** | VERIFIED | "A corpus of Studio outputs is a corpus of system outputs, and S1's governing sentence would apply to it verbatim"; and in the reference line, that S1 is the "Source of assertion B4, the information budget of two to three independent state channels per stave and about 6.6 recoverable bits per character per frame" | Opened. **Revision 3 is current.** The governing sentence is verified: section 6.1 at line 420 applies it as described. B4 is in S1 at line 317, and so is the two-to-three channel figure, which S1 states with a hedge the reference drops: "The working notes put that at two to three per stave ... and that number is an estimate rather than a measured figure; S3 owes either its derivation or the recovery study that replaces it." **Not supported: the 6.6 bits.** The string does not appear anywhere in S1 revision 3. The figure originates at `REVIEW-A-constraints.md:309`, where it is derived as $\log_2(8 \times 3 \times 4) \approx 6.6$, and is carried into `ASSERTIONS-REGISTER.md:213` as part of B4's constraint clause. Both are in the series and neither is S1 | Sections 5a.3 and 6.2, where the budget is the cap on simultaneous quantities. **Repaired.** The reference line now attributes only the two-to-three channel figure to S1, quotes S1's hedge at `:317` verbatim, and states that the 6.6 is not S1's, giving `REVIEW-A-constraints.md:309` for its derivation and `ASSERTIONS-REGISTER.md:213` for the carry into B4. The cap is unaffected; the body at `:485` already carried the hedge and now the reference line does too |
| 2 | MPN-S2, `S2-mathematics.md`, revision 4 with its post-acceptance correction. Series document | SELF | In the reference line, "Lemma 2, the tripod, is section 4.3" | Opened. **Revision 4 is current.** Section 4.3 is at line 106, headed "The dominant register map is discontinuous, and the mode flickers", and Lemma 2 is stated at line 110: the $\arg\max$ map is discontinuous "precisely on the three segments joining the barycentre to the three edge midpoints, a tripod". The section number and the name are both exact | Section 5a and the decision B extension of the pre-image precedent to mode. The tripod is what makes a tie rule necessary rather than optional |
| 3 | MPN-S3, `S3-mapping-phi.md`, revision 9, as amended by `MPN-S3-AMENDMENTS-2.md` of 14 September 2026. Series documents | SELF | Section-by-section: 2.1 the dynamics law, 2.2 the tempo bands and metre lookup, 2.3 the tie rule and interpolation, 2.4 the two texture ladders at even fifths, 2.5 the harmonic parameter "now in the absolute form at $k_{\max} = 23$", 2.6 the timbre map with its rank and nullity, section 3 identifiability, section 4 the bias layer with sixteen drafted mappings and fourteen inherited devices, section 5 the influence layer | Both opened. S3's status line reads "Revision 9"; **revision 9 is current.** All nine cited sections exist at the numbers given: 2.1 at line 69, 2.2 at 101, 2.3 at 135, 2.4 at 220, 2.5 at 234, 2.6 at 270, 3 at 303, 4 at 372, 5 at 472. `MPN-S3-AMENDMENTS-2.md` exists and its "Applies to" field names S3 revision 9, sections 2.3, 2.5, 3, 4 and 5, which is the amendment the reference invokes and is what puts $k_{\max}$ into the absolute form | Most of the document. This is the most heavily used reference at 37 body citations, and sections 5a, 5b and 6.2 are S3 applied |
| 4 | MPN-S4, `S4-application.md`, revision 2, with `ARBITRATION-S4.md`. Series documents | SELF | "the parser assigned 3,424 of King Lear's 3,425 rows to a non-speaker, and the speaker-change column there 'is constant by construction'"; and in the reference line, that section 2 establishes the provenance of the score columns, 3.2 the anthologies of eight, five and three works, 3.3 the King Lear parse failure, 3.5 the naming ruling, section 4 the Gutenberg licence finding, and S4-1 the committed credential | Both opened. **Revision 2 is current.** Every anchor checks: section 2 at line 72 ("There are two programs, not one"); 3.2 at 139 with the eight, five and three works enumerated at line 41; 3.3 at 167 with "3,425 rows. **3,424 of them carry the speaker `STAGE`**" at 169, and the single other value `FINIS`; 3.5 at 179; section 4 at 185, carrying the licence finding at 177 where the engine "scores a copyright notice as a catastrophe" for 268 and 269 consecutive rows; S4-1 at lines 37 and 345. **Independently confirmed against the corpus**: `KING_LEAR.csv` has exactly 3,425 data rows, and the seven files total exactly 31,078 | Sections 6.2, 8a and 10. The exclusion rule the document owes its corpus work comes from here, and the three-clef legibility figures are computed over these rows |
| 5 | MPN-PRD-01, `PRD-MPN-THERAPY.md`, draft 6. Series document. **Reference line corrected in the repair pass of 14 September 2026** | VERIFIED | "The PRD's own evidence records the major-happy and minor-sad association as robust and reaching 92 per cent in adults"; and in the reference line, that "Section 5.2 is the paradigm client who responds to the material itself; 6.2 requires every optional input to be off by default and binds each input separately; 6.3 is the Autonomy-to-register table section 11 rules on; 7.4 to 7.6 are the therapist's setup budget and the audible-fraction wording; section 14 is the evidence on the major-happy and minor-sad association reaching 92 per cent in adults" | Opened. **Draft 6 is current.** Three anchors are exact: 6.2 at line 203 requires that "the product is fully usable with every optional input switched off, and ships with all of them off by default ... and it binds each input separately rather than the analyser as a class"; 6.3 at line 209 is the Autonomy-to-register mapping table; 7.4 to 7.6 exist at 292, 302 and 310 and carry the setup budget and the audible fraction. **Two anchors are wrong.** Section 5.2 is headed "What the therapist sees" and is a product table; the paradigm client appears at line 314, inside 7.6, and again at 470 inside section 14. And section 14 is headed "Open questions for the author"; the 92 per cent evidence is at line 261, inside section 7.2, "The mode, and an honest constraint". The claims are true and two of the five section numbers pointed elsewhere. **Repaired**: the reference line now gives 7.6 at `:314` for the paradigm client and 7.2 at `:261` for the 92 per cent, and records the two numbers it corrects | Sections 5b, 6.3 and 11. The Autonomy ruling of section 11 depends on 6.3, which is anchored correctly; the mode argument of section 5b depends on the 92 per cent, which is in the PRD but not where this reference says |
| 6 | Bruscia, Improvisation Assessment Profiles, 1987, as summarised at `ifas.thws.de`, and cited in [5]. Web page summarising a published book | VERIFIED | "Bruscia defines Autonomy over the relationship between improvisers, so it is naturally recorded as a directed edge"; and in the reference line, "Six profiles, five gradients each; the Autonomy gradients are Dependent, Follower, Partner, Leader, Resister" | Fetched at `ifas.thws.de/en/high-m/theory/improvisation-assessment-profiles-iap/`. Six profiles, Integration, Variability, Tension, Congruence, Salience and Autonomy, each rated on "a scale of 5 discrete gradients"; the Autonomy gradients returned verbatim as Dependent, Follower, Partner, Leader and Resister; and Autonomy described as dealing with "the kinds of role relationships formed between the improvisers", which is exactly the relational definition the document turns into a directed edge. Every element confirmed | Sections 5a.1 and 11, and the influence layer's between-stave relation channel. It is the only external source in the document and it carries the ruling of section 11 |
| 7 | The carry-over manifest, `05_DATA/03_generators/s5_salvage.py`. Script. **Path supplied and the unresolved marking withdrawn in the repair pass of 14 September 2026** | VERIFIED | "Nineteen artefacts: four ported, seven repaired, two rewritten, six dropped"; and that "Revision 7 cited it at `05_DATA/03_generators/`, which the Constraint Guardian established does not hold it, and no verified path for it appears in [13]. The citation is therefore recorded as unresolved rather than given a path this revision cannot confirm" | **The counts are exact.** The script exists and was opened; it declares nineteen artefact entries and their verdicts are PORT 4, REPAIR 7, REWRITE 2 and DROP 6, totalling nineteen, which matches the reference word for word. **Not supported: that no path can be confirmed.** The file resolves at `/home/claude/gen4/s5_salvage.py`, in the same directory as the nine S4 generators, and nowhere else. The document is right that `05_DATA/03_generators/` does not hold it, and right to refuse a path it had not checked; the path exists and this ledger supplies it. Note also that this reference is never cited in the body, so its four counts reach the reader only through the reference list | Section 10a, the implementation order, and change 43. **Repaired.** The script is committed in the live corpus generators directory, 7,098 bytes, MD5 `08f749308599ca0cc628430ee8351781`, byte for byte the working copy at `/home/claude/gen4/s5_salvage.py`. Reference [7] now gives the corpus path and says why the earlier refusal was right at the time; the change 43 row in section 13 is updated to match |
| 8 | The six review files of revisions 7 and 1 to 5. Series documents | SELF | "The earlier reviews of revisions 1 to 5 stand where the arbitration did not reach them"; and that revision 7 drew STOP with 11 blocking of 20, REVISE with 8 of 12, and REVISE with 10 of 13 | All six opened and present in the drafts directory: `REVIEW-DESIGN-R7-skeptic.md`, `REVIEW-DESIGN-R7-constraints.md`, `REVIEW-DESIGN-R7-user.md`, `REVIEW-DESIGN-skeptic.md`, `REVIEW-DESIGN-constraints.md`, `REVIEW-DESIGN-user.md`. The three dispositions and blocking counts for revision 7 are corroborated at `ARBITRATION-DESIGN-R7.md:7`, which states them identically. No revision numbers are carried on the review files themselves | Section 9's decision log and the void marking on the Guardian's findings 7 to 11. A reader auditing the scope ruling needs the older three |
| 9 | The bias layer, `gen6/s6_bias_layer.py`, run against `gen6/s3_bias_reconciliation.json`. Script | VERIFIED | "What was missing was not permission but the three things a layer has to survive before anyone builds it, which `s6_bias_layer.py` computes from the reconciliation rather than restating"; and that the JSON "is now committed beside it so the script runs from the corpus alone with no environment variable"; and that "Revision 7 cited it at `05_DATA/03_generators/`, corrected here" | Both files opened at `/home/claude/gen6/`. The script resolves its input at :60-63 by trying `MPN_RECON`, then `os.path.join(here, 's3_bias_reconciliation.json')`, then two home-directory fallbacks, so the committed sibling is found with no environment variable set, exactly as claimed. `s3_bias_reconciliation.json` is present in the same directory. The corrected prefix `gen6/` is the prefix that resolves | **Path repointed 14 September 2026.** The corpus generators directory now holds `s6_bias_layer.py` and `s3_bias_reconciliation.json`, so the reference gives `05_DATA/03_generators/` rather than the `gen6/` working copy, and records that the two are the same file. Section 5a entire, the bias layer. The coordinate census, the eight collisions and the twenty-two by eight locality partition all come from this script |
| 10 | The timbre channel, `gen6/s6_timbre_capacity.py`. Script | VERIFIED | "`s6_timbre_capacity.py` answers the first conditionally and the second exactly"; and that it "repeats the whole table at three seeds under a flag" | Opened at `/home/claude/gen6/s6_timbre_capacity.py`. It seeds at :58 with `random.seed(20260913)`, documents the `--reseed` flag at :42 as repeating the whole table at three seeds and asserting agreement, and records at :46 the progression of the seven-character figure from 1.0123 published through 1.0360 under the Skeptic's search to 1.053712 under the arbitration's. The exhaustive corner search that needs no seed is at :203. The path resolves | **Path repointed 14 September 2026** to `05_DATA/03_generators/s6_timbre_capacity.py`, which is the path the PRD gives at its own [18] and which now resolves. Section 5b entire. The separation curve, the fibre lengths and the family conflict are all its output, and the capacity claim revision 8 strikes was struck on its evidence |
| 11 | `gen6/s6_three_clef.py`. Script | VERIFIED-NARROWED | "It still asserts a total of 31,078 rows, which enforces the contamination rather than guarding against it; it still calls the rows beats throughout; and its default corpus path does not resolve" | Opened at `/home/claude/gen6/s6_three_clef.py`. **Two of the three hold.** The assertion is at :164, `assert ROWS_READ == 31078`, which does enforce the full contaminated set. The default corpus path does not resolve: the three candidates at :61-63 all point into `eigenia/papers-pre-publish/MPN/05_DATA/01_scores` under prefixes that are absent here, and only `MPN_SCORES` makes it work. **The third no longer holds.** The word "beat" appears five times in the file, at :16, :19 and :129, and every occurrence is a note repudiating the usage and quoting S4 section 3.5: "Rows, not beats: the word beat is reserved". The file's modification time is 17:20 on 14 September and this revision's is 17:16, so the script was corrected four minutes after the document was written, and the reference is a true statement that has been overtaken rather than an error | **Path repointed 14 September 2026** to `05_DATA/03_generators/s6_three_clef.py`, and the reference now records that the beats defect was corrected at 17:20 on 14 September, four minutes after this revision, while the row assertion and the unresolved default path are still open. Section 6.2's three-clef figures and section 10's outstanding work. Two of the three defects the document flags are still open and should stay on the list; the third can be struck |
| 12 | The corpus recount, `gen6/s6_corpus_recount.py`. Script | VERIFIED-NARROWED | "added 14 September 2026 and independent of [11], re-reading the score files and recomputing from scratch" | Opened at `/home/claude/gen6/s6_corpus_recount.py`. The date is confirmed at :5, which reads "Written 14 September 2026 as a CHECK on `s6_three_clef.py`, not as a" continuation, and the script does re-read the score files and recompute. **Independence is narrower than the word suggests**: at :20 the script states that "The laws are S3's normative ones, restated identically to `s6_three_clef.py`", and at :91 it adopts the same speaker convention. It is an independent reading of the data under the same transcribed laws, which catches a reading error and would not catch a shared transcription error | **Path repointed 14 September 2026** to `05_DATA/03_generators/s6_corpus_recount.py`, and the reference now states the independence at its true width, naming `:20` where the script restates S3's laws identically. Section 6.2's coverage and event-density figures, and the entropy census. The check is real and worth having; a reader should not take it as an independent derivation of the laws |
| 13 | `gen6/VERIFICATION-2026-09-14.txt`. Verification record | VERIFIED | "Every figure in this revision that is not from a named paper is confirmed or corrected there, with the command that produced it"; and that it corrects the arbitration three times, the seven-character timbre value moving "from 1.053712 to 1.089845 and 1.053712 did not reproduce at any seed tried", the four-character value "from 1.4231 to 1.424955", and "falls 28 per cent at the seventh" being "deleted rather than corrected" | Opened at `/home/claude/gen6/VERIFICATION-2026-09-14.txt`, 52,752 bytes. Entry E.4 at :428 is headed "1.053712 AT N = 7. CORRECTED -- IT IS NOT REPRODUCIBLE AND IT IS LOW", and at :459 to :470 records the correction to 1.089845, states that 1.053712 "DOES NOT REPRODUCE" under the arbitration's own stated command, and reports 1.089845 "identically at all three" seeds. The four-character move from 1.414214 to 1.424955 is tabulated at :444. All three corrections confirmed | **Path repointed 14 September 2026** to `05_DATA/03_generators/VERIFICATION-2026-09-14.txt`, in the reference and in the status block. Every number in the document that is not from a paper, by the status block's own statement. This is the document's verification backbone and it holds |
| 14 | `ARBITRATION-DESIGN-R7.md`, 14 September 2026, disposition STOP. Series document | SELF | "`ARBITRATION-DESIGN-R7.md` ruled forty-five findings, upheld twenty, upheld and narrowed twenty-two, rejected three, and returned **STOP**" | Opened. The disposition field at :13 reads STOP; section 1 at :21 is headed "Rulings on all forty-five findings"; the preamble at :17 states that "Three findings are rejected outright"; and the ruled reviews at :7 are the three of [8] with the blocking counts the status block gives. Twenty plus twenty-two plus three is forty-five, and the arithmetic closes | Section 13's change audit, which claims applied 42, applied in part 2, discharged elsewhere 1, against these forty-five. The document's central claim to have met its gate rests here |
| 15 | `DECISION-LOG-2026-09-14.md`, 14 September 2026. Series document | SELF | "BL-8 is answered as option (e), so the layer occupies no musical channel and is a Layer 3 detector emitting a marked textual proposal, with a reopening condition rather than a permanence clause; and both of MPN-NOTE-05's recommendations are adopted" | Opened. Decision D-2026-09-14-A at :9 is headed "BL-8, the bias layer's channel" and decision D-2026-09-14-B at :19 is headed "MPN-NOTE-05, both recommendations". The status line reads "Taken. These are the two decisions the arbitration of revision 7 reserved to the author and said revision 8 must not be drafted without" | Sections 5a, 5b and 6.2, and the $k_{\max} = 23$ restatement. Without decision B the harmonic parameter would still be in the relative form |
| 16 | `MPN-NOTE-05-blocking-numbers.md`, 14 September 2026. Series document, cited by bare filename | SELF | "headed an estimate for the author's decision and not a ruling"; and that "Twenty-three is the only value at which the absolute and relative forms have the same reachable set" | Opened. Its status field reads "Estimate for the author's decision. Not a ruling", which the reference reproduces exactly. **The file is not in the drafts directory and not in `08_PAPERS`**; it resolves only at `/home/claude/gen7/MPN-NOTE-05-blocking-numbers.md`. Since the citation gives no path, nothing is broken as written, but a reader looking for it beside the other references will not find it. Its own reproducibility line names `05_DATA/03_generators/s7_blocking_numbers.py`, which does not resolve either; that script is at `/home/claude/gen7/s7_blocking_numbers.py` | Section 5a's blocking numbers and the $k_{\max}$ constant. Ten body citations depend on a file the reference list does not locate |
| 17 | `ASSERTIONS-REGISTER.md`. Series document | SELF | "B4 claims that profile, state and bias are simultaneously legible because they occupy channels that do not compete, and it puts bias on the harmonic relation between staves rather than on any channel of a single stave; its falsification clause names the failure verbatim, the between-stave channel turning out to be a within-stave channel in disguise"; and that B3 "indexes dyadic biases by speaker, other and turn and individual biases by speaker and turn" | Opened. B1 at :173, B3 at :193 and B4 at :207. B3's formal content at :197 reads "For dyadic biases, b is indexed by speaker, other and turn. For individual biases, by speaker and turn", which is the reference's sentence. B4's claim at :209 and its falsification clause at :215 both carry the quoted wording, including "the between-stave channel turns out to be a within-stave channel in disguise". Exact on every element | Sections 5a.3 and 6.2. B4's falsification clause is what section 5a.3's census is built to avoid triggering, so the verbatim match matters |
| 18 | `MPN-S3-BL8-OPTIONS.md`, 14 September 2026. Series document | SELF | "the candidate is the **fourth stave** and not articulation and voicing"; and "The domain census over the completed thirty is Perception 8, Decision 10, Social 6 and Memory 6"; and in the reference line, "Five candidates against five tests, three live, option (e) recommended with a reopening condition" | Opened. Section 2 at :24 is headed "The five candidates against the five tests", which confirms the counts, and the recommendation of option (e) is corroborated independently by decision A of [15]. The document's role line, "Claude drafts options, Jim McKenney rules", is its own and is carried | Sections 5a.7, 5a.8 and 6.2. The reopening condition is what keeps the fourth stave available, and section 5a's scope depends on it |
| 19 | `MPN-S3-BL1-DRAFT.md`, 14 September 2026. Series document | SELF | "A state coordinate is attached to a cognitive bias in exactly three places in that checkout, and not one of them is a coordinate for any single entry"; and in the reference line, "The fourteen drafted coordinates with three rows flagged, the seven-part search of the reference implementation and what it found" | Opened. Its item line reads "BL-1, the state coordinate for the fourteen inherited mappings", and its metadata gives "Rows flagged as genuinely undecided | 3", which are the fourteen and the three exactly. The search table runs to S4 and beyond with a row per part. Its finding at :45 on the trauma gate in `lookupArticulation` at `mpn_reference_lookup.ts:183` independently corroborates the anchor cited at [21] | Section 5a.1 to 5a.5. The coordinate census and the eight collisions are computed over these fourteen rows |
| 20 | `MPN-S3-BL2-DRAFT.md`, 14 September 2026. Series document | SELF | "the entry is provisionally remapped on Option A, the same pitch classes restated in a different registral disposition and spacing, open and high against close and low, with mode, key, tempo, metre, dynamic marking and instrument all held"; and "Under BL-2's provisional Option A remap the partition becomes twenty-one and nine" | Opened. Its recommendation line reads "Option A, the remap, adopted as provisional pending BL-8, with the condition stated in section 6", which is the provisional status the document relies on. Section 3 at :40 is "Option A, the remap" and section 4 at :62 is "What Option A does not escape", the two costs the reference names | Section 5a.5's collision count and the twenty-one by nine partition. Both move if Option A is withdrawn, which BL-2 says is possible pending BL-8 |
| 21 | `mpn-conductor-standalone`, inspected 14 September 2026, commit `8a3db9f`. Implementation, twelve anchors across six files after the repair pass | VERIFIED. **Corrected in the repair pass of 14 September 2026, from BROKEN** | "Both halves were checked against the reference implementation at path and line, and both are wrong"; and in the reference line, the eight anchors enumerated below, plus the note that "Two of the Constraint Guardian's citations give the calculus file's directory as `src/lib`; the line numbers are right and the directory is corrected here" | All eight anchors were opened at the lines cited, in the working tree at commit `8a3db9f`. **Four are exact, three are wrong, and one is right on its line and wrong on its count.** The graded failure is `psychometric_calculus.ts:204`, cited "for the null return of `inferDISC`": there is no function of that name anywhere in that file, and the line is the opening of `discToInstrument`. `inferDISC` is in a different file, `src/lib/play_parser.ts`, opening at :254 and returning null at :261. The document is right that the Guardian's directory correction was needed, and it is the same class of error one line further on. Details in the table below | Sections 4.4, 5a.4 and 5a.5, and the timbre channel's profile typing in 5b. **Repaired.** All three wrong anchors are replaced, in the reference line and in the body at `:402`, and the reference line records what each formerly said and what is actually at it. The one remaining narrowing, the thirteen-name archetype table cited as fourteen at `page.tsx:96`, is a count and not a locator and is untouched |

## The implementation anchors of [21], as corrected

Checked against `/home/claude/mpn-conductor-standalone` at commit `8a3db9f` of 14 September 2026. The status grades the anchor, not the claim.

| Anchor as cited | Status | What is at that line, and where the claim actually lives |
|:---|:---|:---|
| `src/lib/play_parser.ts:254-262`, for the null return of `inferDISC`. **Corrected in the repair pass, from `psychometric_calculus.ts:204`** | VERIFIED | `inferDISC` opens at :254 and returns null at :261, under a comment recording that the previous implementation "banded four values off average trauma and filled the band with `Math.random()`". The former anchor, `psychometric_calculus.ts:204`, opens `discToInstrument`, whose own null handling is `if (!disc) return 'piano';` at :208, and no function named `inferDISC` exists in that file |
| `psychometric_calculus.ts:310`, for the confirmation-bias wiring into the mode selector | VERIFIED | Exact. Line :310 reads `if (state.biases && state.biases.includes('Confirmation Bias')) likelyEmotion = 'stubborn';`, and the resulting `likelyEmotion` is passed to `lookupMode` three lines later at :313. This is the wiring the document describes |
| `ActorInstrumentPicker.tsx:37` and `:49`, `wizard/TimbreInstrumentPicker.tsx:18` and `:27`, and `psychometric_calculus.ts:213`, for the single-letter profile typing. **Corrected in the repair pass, from `psychometric_calculus.ts:316`** | VERIFIED | All five carry the type `'D' | 'I' | 'S' | 'C'` or that plus `'mixed'`: `ActorInstrumentMapping.discProfile` at :37 and the `actors` prop's `disc` at :49; `ActorInstrument.discProfile` at :18 and the props `disc` at :27; and the cast `const trait = dominant[0] as 'D' | 'I' | 'S' | 'C';` at :213. The body sentence says "the character pickers", so the two picker components are the right sites and the calculus cast is the third. The former anchor, :316, is the call `const instrument = discToInstrument(state.disc, seedKey);` and carries no type |
| `psychometric_calculus.ts:211-212`, for the argmax family selector. **Corrected in the repair pass, from `:326-340`** | VERIFIED | :211 is `const traits = Object.entries(disc);` and :212 is `const dominant = traits.reduce((a, b) => a[1] > b[1] ? a : b);`, which is the argmax over the four DISC coordinates; its result selects the instrument through `lookupInstrument(trait)` at :216. The former range, :326-340, is the base timbre object and the Dark Triad modulation and has nothing to do with selection. **An earlier draft of this row proposed :364-373 instead, and that was wrong**: :364-373 sets `instrumentFamily` by substring match over the instrument name and contains no argmax, so it is not the construct the citation names |
| `page.tsx:96`, for the fourteen-name substitution table | VERIFIED-NARROWED | The line resolves and opens `createActorProfile`, whose archetype table is at :98-112. **The table holds thirteen names, not fourteen**: hamlet, claudius, ophelia, ghost, gertrude, oedipus, jocasta, creon, tiresias, macbeth, lady macbeth, priest and chorus. The count is one high, which matters to section 4.4 only as a figure and not as an argument |
| `page.tsx:115`, for the flat default | VERIFIED | Exact. Line :115 is `const profile = archetypes[key] || {`, with the flat default at :116-117 assigning every DISC coordinate 0.5 and every Dark Triad coordinate 0.3. This is the flat default the document describes |
| `play_parser.ts:254` | VERIFIED | Exact, and it is the anchor that `psychometric_calculus.ts:204` should have carried. Line :254 opens `function inferDISC(_name: string, _avgTrauma: number)`, with the open task recorded in its comment and `return null` at :261. `inferDarkTriad` follows at :264 with the same disposition |
| `mpn_reference_lookup.ts:183-214`, for the trauma gate on the bias list | VERIFIED | Exact. `lookupArticulation` opens at :183, returns `sforzando` above trauma 0.7 at :189 and `marcato` above 0.5 at :190, both before the bias loop is entered at :192, and falls through to `legato` at :214. The gate applies to the list collectively and names no bias, which is the reading BL-1 [19] gives it independently |

## Claims carried without a citation, on purpose

The four surfaces of section 6 are product design and are attributed to nobody, correctly. The failure behaviour of section 8a, the edge cases of section 8 and the constraints of section 7 are decisions rather than findings. Section 10's list of what the design does not yet have is a statement about the document itself and has no source by construction. The one place where an uncited claim would repay a citation is section 5a.6's account of what kind of test each bias admits, which reads as though it follows from the scope ruling and in fact goes a step beyond it.

## Not cited, though available

The four-reviewer gate that produced S4, `ARBITRATION-S4.md`, is cited at [4] but only as a companion to S4 and never for its own rulings, several of which bear on section 6.2's use of the score files. `REVIEW-S3-skeptic.md`, which contains the sharpest statement in the corpus of why the timbre dimensionality result and the channel budget are different quantities with different units of account, is not cited anywhere in this document, and section 5b's argument would be stronger for engaging it. Neither omission is a defect; both are places where the document argues alone when it has company.
